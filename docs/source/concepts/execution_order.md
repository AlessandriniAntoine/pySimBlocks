# Execution Order and Algebraic Loops

This page explains how pySimBlocks determines the order in which blocks are executed each time step, and what happens when a dependency cycle (algebraic loop) is detected.
